#include "main.h"
#include <stdio.h>

/**
 * main - Prints name followed by new line
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 (End)
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
