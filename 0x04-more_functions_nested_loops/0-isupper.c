#include "main.h"
/**
 * _isupper - Check for uppercase character
 * @c: Variable text
 * Return: 0 (End of program)
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
